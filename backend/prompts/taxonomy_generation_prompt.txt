You are an expert legal/compliance taxonomist. 
Your task is to analyze the provided contract section and manual tags to either map them to an existing taxonomy or propose new, unique tags.

### CURRENT TAXONOMY (JSON)
{current_taxonomy}

### INPUT SECTION
Text: "{section_text}"
Manual Tags: {manual_tags}

### INSTRUCTIONS
1. **Reuse First**: If the concepts in the section map clearly to existing tags in the CURRENT TAXONOMY, reuse their `tag_id`.
2. **Define New**: If a concept is fundamentally new and not covered by existing tags, define a NEW tag.
3. **Format**: Return a JSON list of tags used in this section. For new tags, provide:
   - `tag_id`: A semantic, UPPER_CASE_SLUG (e.g., PAYMENT_TERMS). **DO NOT use prefixes like "TAG_" or "NEW_TAG_".**
   - `display_name`: Human-readable name.
   - `description`: A clear, concise definition.
5. **Definition Style (CRITICAL)**:
   - **DO NOT** start definitions with "Defines the...", "This tag represents...", or similar meta-phrasing.
   - **DO** use direct, noun-phrase definitions.
   - *Bad*: "Defines the specific agreement, including appendices."
   - *Good*: "The specific agreement, including appendices and incorporated documents."
   - *Bad*: "Defines the prohibition of offering bribes..."
   - *Good*: "The prohibition of offering, authorizing, soliciting, accepting, or giving anything of value to improperly influence a party."
6. **Uniqueness**: Ensure no duplicate definitions exist for the same concept.

### OUTPUT FORMAT
Return ONLY a JSON list of tag objects.
Example:
[
  {{ "tag_id": "EXISTING_CONCEPT", "display_name": "...", "description": "..." }},
  {{ "tag_id": "NEW_CONCEPT", "display_name": "...", "description": "..." }}
]
